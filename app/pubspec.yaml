name: judge
description: judge J-League player.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.15.0-35.0.dev <3.0.0"
dependencies:
  flutter:
    sdk: flutter
  # firebase
  firebase_core: ^1.10.6
  firebase_auth: ^3.3.7
  cloud_firestore: ^3.1.5
  # other
  cupertino_icons: ^1.0.2
  intl: ^0.17.0
  provider: ^6.0.2
  badges: ^2.0.1
  flutter_stripe: ^4.0.0
  flutter_dotenv: ^5.0.2
  flutter_svg: ^1.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true
  assets:
    - lib/_images/
    - lib/.env # For Stripe